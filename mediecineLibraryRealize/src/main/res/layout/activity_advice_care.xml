<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >
  <include layout="@layout/titlebars_patient"
      android:id="@+id/titlebars_patient"/>
 <!--  <include layout="@layout/layout_mybox_title"/> -->


    <ListView android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:layout_below="@+id/titlebars_patient"
        android:layout_above="@id/rl_des"
    android:paddingLeft="10dp"
    android:paddingRight="10dp"
    android:scrollbars="none"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:cacheColorHint="#00000000"
        android:listSelector="#00000000"

        android:divider="@color/translate"
    android:dividerHeight="0dp"
    android:id="@+id/listview"></ListView>
</RelativeLayout>
